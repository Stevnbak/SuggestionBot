{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bun",
            "request": "launch",
            "name": "Debug Bot",
            //"preLaunchTask": "build",
            "program": "src/app.ts",
            "cwd": "${workspaceFolder}",
            "env": { "NODE_ENV": "development" },
            "runtime": "bun"
        }
    ]
}
