{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bun",
            "request": "launch",
            "name": "Debug Bot",
            "program": "app.ts",
            "cwd": "${workspaceFolder}/src",
            "env": { "NODE_ENV": "development" },
            "runtime": "bun"
        }
    ]
}
